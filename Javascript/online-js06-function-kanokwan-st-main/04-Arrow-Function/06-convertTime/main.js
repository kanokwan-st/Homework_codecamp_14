const dayToSec = (day) => sec = day * 24 * 60 * 60;

console.log(dayToSec(1));



/* 
    const dayToSec = (day) => {
        let sec = day * 24 * 60 * 60;
        return sec;
    }

    console.log(dayToSec(1));
*/
